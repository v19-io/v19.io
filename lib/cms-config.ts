// CMS site ID registered in the content KV store.
// Set this to the siteId from /api/cms/sites (or the legacy content-api admin).
export const CMS_SITE_ID = "v19-io"

// Base URL for the CMS API. Points to the local Next.js routes.
// In production this is the same domain — no separate Worker needed.
export const CMS_API_URL = "/api/cms"
